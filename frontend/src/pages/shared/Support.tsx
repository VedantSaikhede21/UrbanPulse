import React from 'react';
import { PageStub } from '../../components/ui/PageStub';

export const Support: React.FC = () => {
  return (
    <PageStub
      title="Help & Support Desk"
      description="Access user manuals, read standard operating procedures (SOPs) for field resolutions, or contact support."
      role="Shared"
      milestone="M1"
    />
  );
};
