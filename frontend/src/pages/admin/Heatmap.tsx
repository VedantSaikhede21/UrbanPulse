import React from 'react';
import { PageStub } from '../../components/ui/PageStub';

export const Heatmap: React.FC = () => {
  return (
    <PageStub
      title="Geospatial Heatmap"
      description="Interactive CartoDB dark-matter Leaflet map showcasing active incident density, filtered by category and urgency."
      role="Ward/City Admin"
      milestone="M8"
    />
  );
};
