import React from 'react';
import { PageStub } from '../../components/ui/PageStub';

export const RoutingConfig: React.FC = () => {
  return (
    <PageStub
      title="Department Routing Rules"
      description="Edit mapping variables that associate ticket categories dynamically to specific departments without code changes."
      role="Super Admin"
      milestone="M8"
    />
  );
};
